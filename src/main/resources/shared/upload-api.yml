app:
  azure:
    storage:
      accountName: blank
      accountKey: blank

spring:
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 10MB

---
spring:
  config:
    activate:
      on-profile: prod
